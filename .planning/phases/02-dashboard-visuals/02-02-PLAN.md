---
phase: 02-dashboard-visuals
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - fintrack-dashboard/src/components/dashboard/spending-chart.tsx
  - fintrack-dashboard/src/components/dashboard/category-chart.tsx
  - fintrack-dashboard/src/components/dashboard/category-transactions.tsx
  - fintrack-dashboard/src/app/(app)/page.tsx
autonomous: false
requirements:
  - CHRT-01
  - CHRT-02
  - CHRT-03
  - CHRT-04

must_haves:
  truths:
    - "User sees a 6-month spending trend bar chart and can hover any bar to see the exact amount"
    - "User sees a category donut chart for current month spending with legend"
    - "User can click any donut category to see its transactions"
    - "Charts use muted harmonious palette with 10+ distinct category colors"
    - "All dashboard sections (summary, accounts, charts) render on the page with real data"
  artifacts:
    - path: "fintrack-dashboard/src/components/dashboard/spending-chart.tsx"
      provides: "6-month spending bar chart with hover tooltips"
      exports: ["default"]
    - path: "fintrack-dashboard/src/components/dashboard/category-chart.tsx"
      provides: "Category donut chart with legend and click handler"
      exports: ["default"]
    - path: "fintrack-dashboard/src/components/dashboard/category-transactions.tsx"
      provides: "Drill-down transaction list for selected category"
      exports: ["CategoryTransactions"]
    - path: "fintrack-dashboard/src/app/(app)/page.tsx"
      provides: "Orchestrates all dashboard sections with real data"
  key_links:
    - from: "fintrack-dashboard/src/components/dashboard/spending-chart.tsx"
      to: "getTrailingMonthlySpending"
      via: "data prop passed from page.tsx server component"
      pattern: "data.*month.*total"
    - from: "fintrack-dashboard/src/components/dashboard/category-chart.tsx"
      to: "getCategorySpending"
      via: "data prop passed from page.tsx server component"
      pattern: "data.*category.*total"
    - from: "fintrack-dashboard/src/components/dashboard/category-chart.tsx"
      to: "fintrack-dashboard/src/components/dashboard/category-transactions.tsx"
      via: "click handler passes selected category"
      pattern: "onCategoryClick|setSelectedCategory"
    - from: "fintrack-dashboard/src/app/(app)/page.tsx"
      to: "all dashboard components"
      via: "dynamic imports and direct server component imports"
      pattern: "dynamic.*import|SummaryCards|AccountCards"
---

<objective>
Build the spending trend bar chart and category donut chart with drill-down, then wire ALL dashboard components (from Plan 01 and this plan) into page.tsx replacing the placeholder sections.

Purpose: Complete the visual dashboard with interactive charts and connect everything to the page layout so users see their real financial data.
Output: 3 new chart/drill-down components, updated page.tsx with all dashboard sections populated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-visuals/02-RESEARCH.md
@.planning/phases/02-dashboard-visuals/02-CONTEXT.md
@.planning/phases/02-dashboard-visuals/02-01-SUMMARY.md

@fintrack-dashboard/src/app/(app)/page.tsx
@fintrack-dashboard/src/lib/queries/types.ts
@fintrack-dashboard/src/lib/plaid-amounts.ts

<interfaces>
<!-- From Plan 01 outputs - executor should verify these exist -->

From src/lib/queries/transactions.ts (added in Plan 01):
```typescript
export async function getTrailingMonthlySpending(months?: number): Promise<{ month: string; total: number; count: number }[]>
export async function getCategorySpending(yearMonth: string): Promise<{ category: string; total: number; count: number }[]>
export async function getTransactionsByCategory(yearMonth: string, category: string): Promise<Transaction[]>
```

From src/lib/chart-colors.ts (created in Plan 01):
```typescript
export const CATEGORY_COLORS: Record<string, string>
export function getCategoryColor(category: string): string
```

From src/components/ui/chart.tsx (installed in Plan 01 via shadcn):
```typescript
export { ChartContainer, ChartTooltip, ChartTooltipContent, ChartLegend, ChartLegendContent }
export type { ChartConfig }
```

From src/components/dashboard/ (created in Plan 01):
```typescript
// summary-cards.tsx
export { SummaryCards } // async server component
// net-position-card.tsx
export { NetPositionCard } // async server component
// account-cards.tsx
export { AccountCards } // async server component
```

From src/lib/plaid-amounts.ts:
```typescript
export function formatCurrency(amount: number): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build spending trend bar chart and category donut chart with drill-down</name>
  <files>
    fintrack-dashboard/src/components/dashboard/spending-chart.tsx,
    fintrack-dashboard/src/components/dashboard/category-chart.tsx,
    fintrack-dashboard/src/components/dashboard/category-transactions.tsx
  </files>
  <action>
    Use Stitch MCP (Nano Banana 2) to generate chart designs for a fintech dashboard, then implement following the ui-ux-pro-max skill and the shadcn chart patterns from RESEARCH.md.

    1. Create `src/components/dashboard/spending-chart.tsx` as a "use client" component:
       - Default export for dynamic import compatibility
       - Props: `{ data: { month: string; total: number; count: number }[] }`
       - Use shadcn `ChartContainer` with a `ChartConfig` that maps "spending" to a teal/cyan HSL color
       - Render Recharts `BarChart` with:
         a. `CartesianGrid` with vertical={false}, strokeDasharray="3 3", subtle stroke color
         b. `XAxis` with dataKey="month" (short month names like "Jan")
         c. `YAxis` with tick formatter using formatCurrency (abbreviated: "$1.2k" format for axis labels)
         d. `ChartTooltip` with `ChartTooltipContent` showing month name and exact dollar amount via formatCurrency
         e. `Bar` with dataKey="total", fill using CSS variable from ChartConfig, rounded top corners radius={[4, 4, 0, 0]}
       - Responsive: min-h-[300px] on desktop, min-h-[250px] on mobile
       - Dark theme: muted grid lines, light text on axes, teal bar fill
       - Handle empty data: show "No spending data available" message

    2. Create `src/components/dashboard/category-chart.tsx` as a "use client" component:
       - Default export for dynamic import compatibility
       - Props: `{ data: { category: string; total: number; count: number }[], yearMonth: string }`
       - Use Recharts `PieChart` with `Pie` (donut via innerRadius/outerRadius)
       - Map each category to its color from `getCategoryColor()` in chart-colors.ts
       - Create a ChartConfig dynamically from the data categories
       - Render with:
         a. `Pie` with dataKey="total", nameKey="category", innerRadius={60}, outerRadius={100} (donut shape)
         b. Each cell colored by getCategoryColor(category)
         c. `ChartTooltip` showing category name (prettified: "Food & Drink" not "FOOD_AND_DRINK") and amount
         d. Legend below the chart showing category name + color swatch
       - On category segment click: set selectedCategory state, show CategoryTransactions sheet/panel
       - Add a "prettify category" helper: replace underscores with spaces, title case (e.g., FOOD_AND_DRINK -> "Food & Drink")
       - Dark theme: chart on dark background, legend text in muted-foreground
       - Handle empty data: "No spending data for this month"

    3. Create `src/components/dashboard/category-transactions.tsx` as a "use client" component:
       - Export `CategoryTransactions` as named export
       - Props: `{ category: string; yearMonth: string; open: boolean; onClose: () => void }`
       - Use shadcn Sheet component (already installed) for slide-out panel
       - When open, fetch transactions via `/api/category-transactions?yearMonth={}&category={}` -- BUT since we're keeping data fetching server-side, instead pass transactions as a prop OR use a simpler approach:
         **Preferred approach:** The category-chart component should accept ALL current month transactions as a prop (fetched in page.tsx server component). When a category is clicked, filter client-side by category_primary to show in the sheet. This avoids needing a new API route.
       - Display each transaction: merchant_name (fallback to name), formatted amount, date, category badge
       - Sheet header: prettified category name + total spending for that category
       - Sort by date descending
       - Dark theme styling consistent with the rest of the dashboard
  </action>
  <verify>
    <automated>cd fintrack-dashboard && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>spending-chart.tsx renders 6-month bar chart with hover tooltips. category-chart.tsx renders donut chart with legend and click-to-drill-down via Sheet panel showing filtered transactions. All use muted color palette from chart-colors.ts. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire all components into page.tsx with dynamic imports</name>
  <files>
    fintrack-dashboard/src/app/(app)/page.tsx
  </files>
  <action>
    Replace ALL placeholder content in page.tsx with real dashboard components. This is a SERVER component that fetches data and passes it to child components.

    1. Make page.tsx an async server component (add `async` to the function)
    2. Import server components directly:
       - `SummaryCards` from "@/components/dashboard/summary-cards"
       - `NetPositionCard` from "@/components/dashboard/net-position-card"
       - `AccountCards` from "@/components/dashboard/account-cards"
    3. Import chart components via `next/dynamic` with `{ ssr: false }`:
       - `SpendingChart` from "@/components/dashboard/spending-chart"
       - `CategoryChart` from "@/components/dashboard/category-chart"
       - Use `<Skeleton className="h-[300px] w-full rounded-lg" />` as loading fallback for both
    4. Fetch chart data in page.tsx server-side:
       - Call `getTrailingMonthlySpending()` for spending chart data
       - Call `getCategorySpending(currentYearMonth)` for category chart data
       - Call `getTransactions({ limit: 500 })` for category drill-down data (passed to CategoryChart)
       - Use `Promise.all()` for parallel fetching
    5. Update section structure -- KEEP existing section IDs (#summary, #accounts) for MobileNav anchor compatibility:
       ```
       #summary section:
         - SummaryCards (server, self-fetching)
         - NetPositionCard (server, self-fetching)

       #accounts section:
         - AccountCards (server, self-fetching)

       Charts section (new, add id="charts" or similar):
         - SpendingChart (client, receives data as props)
         - CategoryChart (client, receives data + transactions as props)
       ```
    6. Wrap chart sections in a container with section heading "Spending Overview" or similar
    7. Add `<Suspense>` boundaries around each async server component with Skeleton fallbacks
    8. Keep #transactions and #chat sections as placeholders for Phase 3 and 4
    9. Ensure responsive layout: cards sections use grid, charts stack vertically on mobile, sit side-by-side on xl screens
  </action>
  <verify>
    <automated>cd fintrack-dashboard && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>page.tsx imports all dashboard components, fetches chart data server-side, passes to client chart components via dynamic imports. All placeholder text replaced for Summary and Accounts sections. Charts render in a new section. Build passes with no errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete dashboard</name>
  <action>
    Human verifies the complete dashboard renders correctly with real data.
    What was built: Summary cards, account cards, net position, spending trend chart, and category donut chart with drill-down -- all rendering real financial data from Supabase.
  </action>
  <verify>
    1. Run `cd fintrack-dashboard && pnpm dev` and open http://localhost:3000
    2. Log in with the configured password
    3. Verify Summary section:
       - Current month spending shows a dollar amount
       - Last month shows a dollar amount with percentage change (green/red arrow)
       - Transaction count shows a number
       - Net position card shows cash - credit - loans breakdown
    4. Verify Accounts section:
       - Each linked account shows name, last 4 digits, balance, type
       - Credit accounts show utilization bar colored green/amber/red
    5. Verify Charts section:
       - Bar chart shows 6 months of spending with bars
       - Hover a bar -- tooltip shows month name and exact dollar amount
       - Donut chart shows category breakdown with colored segments and legend
       - Click a donut segment -- Sheet slides out showing transactions for that category
    6. Resize to mobile width (375px):
       - Cards stack to single column
       - Charts are readable, labels don't overlap
       - MobileNav anchors (#summary, #accounts) still scroll to correct sections
  </verify>
  <done>User confirms all dashboard sections render correctly with real data, charts are interactive, and mobile layout works. Type "approved" or describe issues to fix.</done>
</task>

</tasks>

<verification>
1. `cd fintrack-dashboard && pnpm build` completes without errors
2. All chart components use "use client" directive and are loaded via dynamic import
3. No Supabase imports in client components
4. Charts use CATEGORY_COLORS from chart-colors.ts
5. Page sections maintain #summary and #accounts IDs for MobileNav compatibility
6. Category drill-down shows filtered transactions in a Sheet
</verification>

<success_criteria>
- Spending trend bar chart renders 6 bars with hover tooltips showing exact amounts
- Category donut chart renders with legend and 10+ distinct muted colors
- Clicking donut segment opens Sheet with that category's transactions
- page.tsx orchestrates all components: summary cards, net position, accounts, charts
- All data fetched server-side, charts receive data as props via dynamic import
- No SSR errors from Recharts (all chart components loaded with ssr: false)
- Mobile layout is readable with no overlapping labels
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-visuals/02-02-SUMMARY.md`
</output>
