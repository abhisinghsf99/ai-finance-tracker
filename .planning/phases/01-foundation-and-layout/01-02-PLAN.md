---
phase: 01-foundation-and-layout
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - fintrack-dashboard/src/app/(app)/layout.tsx
  - fintrack-dashboard/src/app/(app)/page.tsx
  - fintrack-dashboard/src/app/(app)/chat/page.tsx
  - fintrack-dashboard/src/app/(app)/transactions/page.tsx
  - fintrack-dashboard/src/app/(app)/recurring/page.tsx
  - fintrack-dashboard/src/components/layout/top-nav.tsx
  - fintrack-dashboard/src/components/layout/mobile-nav.tsx
  - fintrack-dashboard/src/components/layout/sidebar.tsx
  - fintrack-dashboard/src/__tests__/top-nav.test.tsx
  - fintrack-dashboard/src/__tests__/mobile-nav.test.tsx
  - fintrack-dashboard/src/__tests__/layout.test.tsx
  - fintrack-dashboard/src/app/globals.css
autonomous: true
requirements: [LAYO-01, LAYO-02, LAYO-03]

must_haves:
  truths:
    - "User sees a sticky top nav bar with FinTrack logo on left and Sign Out button on right"
    - "Dashboard is a single scrollable page with section shells (summary, accounts, transactions, chat)"
    - "Desktop layout uses single column with max-width container"
    - "Mobile shows compact top nav and fixed bottom tab bar with anchor links to sections"
    - "Clicking a bottom tab scrolls to the corresponding section without page navigation"
  artifacts:
    - path: "fintrack-dashboard/src/components/layout/top-nav.tsx"
      provides: "Sticky top nav with logo and sign-out button"
      exports: ["TopNav"]
    - path: "fintrack-dashboard/src/components/layout/mobile-nav.tsx"
      provides: "Bottom tab bar with anchor navigation"
      exports: ["MobileNav"]
    - path: "fintrack-dashboard/src/app/(app)/page.tsx"
      provides: "Single-page dashboard with section shells"
      contains: "id=\"summary\""
    - path: "fintrack-dashboard/src/app/(app)/layout.tsx"
      provides: "App layout with TopNav only (no sidebar)"
      contains: "TopNav"
  key_links:
    - from: "fintrack-dashboard/src/components/layout/top-nav.tsx"
      to: "/api/auth DELETE"
      via: "fetch call for sign-out"
      pattern: "fetch.*api/auth.*DELETE"
    - from: "fintrack-dashboard/src/components/layout/mobile-nav.tsx"
      to: "fintrack-dashboard/src/app/(app)/page.tsx"
      via: "anchor links (#summary, #accounts, etc.)"
      pattern: "href.*#(summary|accounts|transactions|chat)"
---

<objective>
Restructure the layout from sidebar + multi-page routing to single scrollable page with top nav bar and mobile bottom tab bar using anchor navigation.

Purpose: Transform the navigation pattern to match the single-page dashboard design. The sidebar gets replaced by a horizontal top nav, separate route pages become section shells on one page, and the mobile bottom tab bar switches from router Links to anchor links.
Output: Working single-page layout with top nav (sign-out functional), mobile bottom tab bar, and section placeholders ready for Phase 2 content.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-layout/01-RESEARCH.md
@.planning/phases/01-foundation-and-layout/01-VALIDATION.md
@.planning/phases/01-foundation-and-layout/01-01-SUMMARY.md

<interfaces>
<!-- Sign-out endpoint created in Plan 01 -->
From fintrack-dashboard/src/app/api/auth/route.ts:
```typescript
export async function DELETE()
// Clears fintrack-session cookie, returns { success: true }
// Call: await fetch("/api/auth", { method: "DELETE" })
```

<!-- Current app layout to be replaced -->
From fintrack-dashboard/src/app/(app)/layout.tsx:
```typescript
// CURRENT: Sidebar + MobileNav + multi-page
// TARGET: TopNav only + single-page with anchor sections
```

<!-- Mobile nav items pattern (change href from routes to anchors) -->
Current navItems use: { href: "/transactions", icon: ArrowLeftRight, label: "Transactions" }
Target navItems use: { href: "#transactions", icon: ArrowLeftRight, label: "Transactions" }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TopNav component + refactor layout + section shells</name>
  <files>
    fintrack-dashboard/src/components/layout/top-nav.tsx,
    fintrack-dashboard/src/components/layout/mobile-nav.tsx,
    fintrack-dashboard/src/components/layout/sidebar.tsx,
    fintrack-dashboard/src/app/(app)/layout.tsx,
    fintrack-dashboard/src/app/(app)/page.tsx,
    fintrack-dashboard/src/app/(app)/chat/page.tsx,
    fintrack-dashboard/src/app/(app)/transactions/page.tsx,
    fintrack-dashboard/src/app/(app)/recurring/page.tsx,
    fintrack-dashboard/src/app/globals.css,
    fintrack-dashboard/src/__tests__/top-nav.test.tsx,
    fintrack-dashboard/src/__tests__/mobile-nav.test.tsx,
    fintrack-dashboard/src/__tests__/layout.test.tsx
  </files>
  <behavior>
    - Test: TopNav renders "FinTrack" text and a "Sign Out" button
    - Test: TopNav sign-out button calls fetch("/api/auth", { method: "DELETE" }) on click
    - Test: MobileNav renders 4 navigation items with href="#summary", "#accounts", "#transactions", "#chat"
    - Test: MobileNav does NOT render any links with href starting with "/" (no route navigation)
    - Test: App layout renders TopNav component (not Sidebar)
    - Test: Dashboard page renders sections with id="summary", id="accounts", id="transactions", id="chat"
    - Test: html element has class "dark" (covered by layout.test.tsx from FOUND-05)
  </behavior>
  <action>
    1. CREATE `src/__tests__/top-nav.test.tsx`: Test that TopNav renders "FinTrack" branding text and a button with text "Sign Out". Test that clicking Sign Out calls fetch with DELETE method to /api/auth (mock fetch, verify call args). Use @testing-library/react render + screen.

    2. CREATE `src/__tests__/mobile-nav.test.tsx` (replace existing): Test that MobileNav renders 4 anchor links with href values "#summary", "#accounts", "#transactions", "#chat". Test that NO links have href starting with "/" (asserting anchor-only navigation). Use @testing-library/react.

    3. CREATE `src/__tests__/layout.test.tsx`: Test that the root html element has class "dark". Test that no ThemeProvider is in the component tree. This covers FOUND-05 validation.

    4. CREATE `src/components/layout/top-nav.tsx`:
       - "use client" (needs onClick handler)
       - Sticky header: `sticky top-0 z-40 border-b border-border bg-background/95 backdrop-blur`
       - Inner div: `mx-auto flex h-14 max-w-6xl items-center justify-between px-4`
       - Left: `<span className="text-lg font-bold text-primary">FinTrack</span>`
       - Right: SignOutButton that calls `fetch("/api/auth", { method: "DELETE" })` then `router.push("/login")` using next/navigation useRouter
       - Use shadcn Button component (variant="ghost", size="sm") with LogOut icon from lucide-react

    5. REWRITE `src/components/layout/mobile-nav.tsx`:
       - "use client"
       - Fixed bottom bar: `fixed bottom-0 left-0 right-0 z-40 border-t border-border bg-background/95 backdrop-blur md:hidden`
       - 4 nav items as `<a>` tags (NOT next/link):
         - { href: "#summary", icon: LayoutDashboard, label: "Summary" }
         - { href: "#accounts", icon: Wallet, label: "Accounts" }
         - { href: "#transactions", icon: ArrowLeftRight, label: "Transactions" }
         - { href: "#chat", icon: MessageSquare, label: "Chat" }
       - Each item: flex column, centered, text-xs, muted-foreground with hover:text-primary
       - Accept className prop for conditional display

    6. DELETE `src/components/layout/sidebar.tsx`

    7. REWRITE `src/app/(app)/layout.tsx`:
       - Import TopNav (not Sidebar)
       - Import MobileNav
       - Structure: TopNav at top, then `<main className="mx-auto max-w-6xl px-4 py-6 pb-20 md:pb-6">{children}</main>`, then MobileNav (md:hidden)
       - No flex h-screen (it is now a scrollable page, not a sidebar layout)

    8. REWRITE `src/app/(app)/page.tsx`:
       - Server Component (no "use client")
       - Render 4 section shells, each with an id and scroll-mt-16 class:
         ```
         <section id="summary" className="scroll-mt-16 mb-8">
           <h2 className="text-xl font-semibold mb-4">Summary</h2>
           <p className="text-muted-foreground">Coming in Phase 2</p>
         </section>
         ```
       - Sections: summary, accounts, transactions, chat
       - Each is a placeholder that will be replaced with real components in later phases

    9. DELETE route pages no longer needed (single-page replaces them):
       - `src/app/(app)/transactions/page.tsx`
       - `src/app/(app)/recurring/page.tsx`
       - `src/app/(app)/chat/page.tsx`
       Also delete the empty directories if possible.

    10. UPDATE `src/app/globals.css`: Add `section[id] { scroll-margin-top: 4rem; }` to handle sticky nav offset for anchor scrolling. Also add `html { scroll-behavior: smooth; }` for smooth scrolling on anchor clicks.

    11. Run tests to verify all pass.
  </action>
  <verify>
    <automated>cd fintrack-dashboard && npx vitest run src/__tests__/top-nav.test.tsx src/__tests__/mobile-nav.test.tsx src/__tests__/layout.test.tsx</automated>
  </verify>
  <done>TopNav shows FinTrack + Sign Out. MobileNav uses anchor links (#summary, #accounts, #transactions, #chat). Sidebar deleted. Single page with 4 section shells. All layout tests pass. Full suite green: `cd fintrack-dashboard && npm test && npm run build`.</done>
</task>

</tasks>

<verification>
cd fintrack-dashboard && npm test && npm run build
</verification>

<success_criteria>
1. TopNav renders with FinTrack logo and functional Sign Out button
2. MobileNav uses anchor links (no route navigation) for 4 sections
3. Sidebar component deleted
4. App layout uses TopNav + scrollable main content (no sidebar flex)
5. Dashboard page has 4 sections with id attributes for anchor targets
6. Separate route pages (/transactions, /recurring, /chat) removed
7. Smooth scrolling works with scroll-margin-top offset
8. `npm test` all green, `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-layout/01-02-SUMMARY.md`
</output>
