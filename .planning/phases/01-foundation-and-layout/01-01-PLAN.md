---
phase: 01-foundation-and-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fintrack-dashboard/src/app/layout.tsx
  - fintrack-dashboard/src/app/api/auth/route.ts
  - fintrack-dashboard/src/components/theme-provider.tsx
  - fintrack-dashboard/src/components/layout/theme-toggle.tsx
  - fintrack-dashboard/src/__tests__/theme-toggle.test.tsx
  - fintrack-dashboard/src/__tests__/sidebar.test.tsx
  - fintrack-dashboard/src/__tests__/auth-api.test.ts
  - fintrack-dashboard/src/__tests__/plaid-amounts.test.ts
  - fintrack-dashboard/src/__tests__/supabase-security.test.ts
  - fintrack-dashboard/src/lib/queries/types.ts
  - fintrack-dashboard/src/lib/queries/accounts.ts
  - fintrack-dashboard/src/lib/queries/transactions.ts
  - fintrack-dashboard/src/lib/queries/institutions.ts
  - fintrack-dashboard/src/lib/plaid-amounts.ts
  - fintrack-dashboard/package.json
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-05]

must_haves:
  truths:
    - "Root layout has class='dark' hardcoded on html element with no ThemeProvider"
    - "Sign-out API route (DELETE /api/auth) clears the session cookie"
    - "Typed query functions exist for accounts, transactions, and institutions"
    - "Plaid amount utilities correctly identify spending vs income and format currency"
    - "No browser-side Supabase imports exist (service_role key stays server-only)"
  artifacts:
    - path: "fintrack-dashboard/src/lib/queries/types.ts"
      provides: "Database row types: Institution, Account, Transaction"
      exports: ["Institution", "Account", "Transaction"]
    - path: "fintrack-dashboard/src/lib/queries/accounts.ts"
      provides: "getAccounts query function"
      exports: ["getAccounts"]
    - path: "fintrack-dashboard/src/lib/queries/transactions.ts"
      provides: "getTransactions, getMonthlySpending query functions"
      exports: ["getTransactions", "getMonthlySpending"]
    - path: "fintrack-dashboard/src/lib/queries/institutions.ts"
      provides: "getInstitutions query function"
      exports: ["getInstitutions"]
    - path: "fintrack-dashboard/src/lib/plaid-amounts.ts"
      provides: "Plaid amount convention utilities"
      exports: ["isSpending", "isIncome", "displayAmount", "formatCurrency", "totalSpending", "totalIncome"]
  key_links:
    - from: "fintrack-dashboard/src/lib/queries/accounts.ts"
      to: "fintrack-dashboard/src/lib/supabase/server.ts"
      via: "createServerSupabase() import"
      pattern: "import.*createServerSupabase.*from.*supabase/server"
    - from: "fintrack-dashboard/src/app/api/auth/route.ts"
      to: "fintrack-session cookie"
      via: "DELETE handler clears cookie"
      pattern: "export async function DELETE"
---

<objective>
Complete the foundation code: sign-out endpoint, dark theme hardcoding (remove next-themes), typed Supabase query layer, and Plaid amount utilities.

Purpose: All backend/library code needed before layout restructuring. FOUND-01 is already done (stack exists). This plan finishes FOUND-02 (sign-out), FOUND-03 (server-only queries), FOUND-04 (typed queries + plaid-amounts), and FOUND-05 (dark-only theme).
Output: Working sign-out API, typed query layer in lib/queries/, plaid-amounts.ts, dark theme hardcoded, tests green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-layout/01-RESEARCH.md
@.planning/phases/01-foundation-and-layout/01-VALIDATION.md

<interfaces>
<!-- Existing server Supabase client used by query layer -->
From fintrack-dashboard/src/lib/supabase/server.ts:
```typescript
export function createServerSupabase()
// Returns a Supabase client using service_role key (server-only)
```

From fintrack-dashboard/src/app/api/auth/route.ts:
```typescript
export async function POST(request: NextRequest)
// Existing login handler - cookie name: "fintrack-session", 30-day maxAge
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dark theme hardcoding + sign-out endpoint + cleanup stale tests</name>
  <files>
    fintrack-dashboard/src/app/layout.tsx,
    fintrack-dashboard/src/app/api/auth/route.ts,
    fintrack-dashboard/src/components/theme-provider.tsx,
    fintrack-dashboard/src/components/layout/theme-toggle.tsx,
    fintrack-dashboard/src/__tests__/theme-toggle.test.tsx,
    fintrack-dashboard/src/__tests__/sidebar.test.tsx,
    fintrack-dashboard/src/__tests__/auth-api.test.ts,
    fintrack-dashboard/package.json
  </files>
  <behavior>
    - Test: DELETE /api/auth returns 200 and sets fintrack-session cookie with maxAge=0
    - Test: Root layout renders html element with class containing "dark"
    - Test: No ThemeProvider component is rendered in the root layout
    - Test: Existing POST /api/auth tests still pass (sign-out does not break login)
  </behavior>
  <action>
    1. UPDATE `src/__tests__/auth-api.test.ts`: Add tests for DELETE /api/auth -- verify it returns {success: true} and sets fintrack-session cookie with maxAge=0, httpOnly=true. Keep existing POST tests unchanged.

    2. DELETE these files (no longer needed):
       - `src/components/theme-provider.tsx`
       - `src/components/layout/theme-toggle.tsx`
       - `src/__tests__/theme-toggle.test.tsx`
       - `src/__tests__/sidebar.test.tsx`

    3. UPDATE `src/app/layout.tsx`:
       - Remove `import { ThemeProvider }` and the ThemeProvider wrapper
       - Change html element to: `<html lang="en" className={\`\${satoshi.variable} dark\`}>`
       - Remove `suppressHydrationWarning`
       - Add `bg-background text-foreground` to body className

    4. UPDATE `src/app/api/auth/route.ts`:
       - Add DELETE handler that clears the fintrack-session cookie (maxAge: 0, httpOnly: true, secure: production, sameSite: lax, path: "/") and returns {success: true}

    5. Run `npm uninstall next-themes` to remove the package

    6. Run tests to confirm RED then GREEN cycle.
  </action>
  <verify>
    <automated>cd fintrack-dashboard && npx vitest run src/__tests__/auth-api.test.ts</automated>
  </verify>
  <done>DELETE /api/auth clears session cookie. Root layout uses hardcoded dark class with no ThemeProvider. next-themes removed from package.json. Old sidebar/theme-toggle tests deleted. All auth tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Typed query layer + Plaid amount utilities</name>
  <files>
    fintrack-dashboard/src/lib/queries/types.ts,
    fintrack-dashboard/src/lib/queries/accounts.ts,
    fintrack-dashboard/src/lib/queries/transactions.ts,
    fintrack-dashboard/src/lib/queries/institutions.ts,
    fintrack-dashboard/src/lib/plaid-amounts.ts,
    fintrack-dashboard/src/__tests__/plaid-amounts.test.ts,
    fintrack-dashboard/src/__tests__/supabase-security.test.ts
  </files>
  <behavior>
    - Test: isSpending(50) returns true, isSpending(-20) returns false
    - Test: isIncome(-30) returns true, isIncome(10) returns false
    - Test: displayAmount(-45.50) returns 45.50, displayAmount(45.50) returns 45.50
    - Test: formatCurrency(1234.5) returns "$1,234.50", formatCurrency(-1234.5) returns "$1,234.50"
    - Test: totalSpending([50, -20, 30, -10]) returns 80
    - Test: totalIncome([50, -20, 30, -10]) returns 30
    - Test: No file in src/app/ or src/components/ imports from @supabase/supabase-js directly (only lib/queries/ and lib/supabase/ may import it)
  </behavior>
  <action>
    1. CREATE `src/__tests__/plaid-amounts.test.ts`: Write tests for all 6 exported functions from plaid-amounts.ts using the behavior cases above. Include edge cases: zero amounts, empty arrays.

    2. CREATE `src/__tests__/supabase-security.test.ts`: Write a test that uses fs to scan all files in src/app/ and src/components/ directories, asserting none import from "@supabase/supabase-js" directly. Only files in lib/ should import the Supabase client.

    3. CREATE `src/lib/queries/types.ts`: Define TypeScript interfaces matching the database schema from the migration SQL:
       - Institution: id, plaid_item_id, institution_name, institution_id, status ("active"|"error"|"login_required"), created_at, updated_at
       - Account: id, plaid_account_id, institution_id, name, official_name, type, subtype, mask, balance_available, balance_current, balance_limit, balance_updated_at, created_at
       - Transaction: id, plaid_transaction_id, account_id, amount, date, datetime, name, merchant_name, merchant_entity_id, category_primary, category_detailed, payment_channel, is_pending, pending_transaction_id, iso_currency_code, logo_url, website, created_at, updated_at
       See exact field types in 01-RESEARCH.md Pattern 6.

    4. CREATE `src/lib/plaid-amounts.ts`: Implement all 6 utility functions exactly as specified in 01-RESEARCH.md Code Examples section. Include the JSDoc comment block explaining Plaid's sign convention.

    5. CREATE `src/lib/queries/accounts.ts`: Implement getAccounts() that calls createServerSupabase().from("accounts").select("*").order("created_at"). Returns Account[]. Throws on error.

    6. CREATE `src/lib/queries/transactions.ts`: Implement:
       - getTransactions(options?: { limit?: number, offset?: number }): Returns Transaction[] ordered by date desc. Default limit 50.
       - getMonthlySpending(yearMonth: string): Filters transactions by date LIKE 'YYYY-MM%', sums positive amounts. Returns { total: number, count: number }.

    7. CREATE `src/lib/queries/institutions.ts`: Implement getInstitutions() returning Institution[].

    8. Run tests to confirm plaid-amounts tests pass and supabase-security test passes.
  </action>
  <verify>
    <automated>cd fintrack-dashboard && npx vitest run src/__tests__/plaid-amounts.test.ts src/__tests__/supabase-security.test.ts</automated>
  </verify>
  <done>lib/queries/ directory exists with types.ts, accounts.ts, transactions.ts, institutions.ts. lib/plaid-amounts.ts exists with 6 exported utility functions. All plaid-amounts tests pass. Supabase security test confirms no browser-side imports. Full test suite green: `cd fintrack-dashboard && npm test`.</done>
</task>

</tasks>

<verification>
cd fintrack-dashboard && npm test && npm run build
</verification>

<success_criteria>
1. `npm test` passes with all auth, plaid-amounts, and supabase-security tests green
2. `npm run build` succeeds with no TypeScript errors
3. next-themes is not in package.json
4. Root layout has hardcoded dark class, no ThemeProvider
5. DELETE /api/auth endpoint clears session cookie
6. lib/queries/ has types.ts, accounts.ts, transactions.ts, institutions.ts
7. lib/plaid-amounts.ts exports isSpending, isIncome, displayAmount, formatCurrency, totalSpending, totalIncome
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-layout/01-01-SUMMARY.md`
</output>
