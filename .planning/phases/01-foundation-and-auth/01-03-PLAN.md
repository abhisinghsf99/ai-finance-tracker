---
phase: 01-foundation-and-auth
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - fintrack-dashboard/src/app/(app)/layout.tsx
  - fintrack-dashboard/src/app/(app)/page.tsx
  - fintrack-dashboard/src/app/(app)/transactions/page.tsx
  - fintrack-dashboard/src/app/(app)/recurring/page.tsx
  - fintrack-dashboard/src/app/(app)/chat/page.tsx
  - fintrack-dashboard/src/components/layout/sidebar.tsx
  - fintrack-dashboard/src/components/layout/mobile-nav.tsx
  - fintrack-dashboard/src/components/layout/theme-toggle.tsx
  - fintrack-dashboard/src/__tests__/sidebar.test.ts
  - fintrack-dashboard/src/__tests__/theme-toggle.test.ts
autonomous: false
requirements:
  - FOUND-04
  - FOUND-05

must_haves:
  truths:
    - "User sees a sidebar with FinTrack branding, 4 nav items (Dashboard, Transactions, Recurring, Chat), and a theme toggle at the bottom"
    - "Active page is indicated by a vertical teal/cyan accent bar on the left edge of the nav item"
    - "Clicking a nav item navigates to the corresponding page"
    - "On mobile viewport, sidebar disappears and a fixed bottom tab bar with 4 icons appears"
    - "Theme toggle switches between dark and light mode"
    - "Each page shows a placeholder/empty state with the page name"
  artifacts:
    - path: "fintrack-dashboard/src/components/layout/sidebar.tsx"
      provides: "Desktop sidebar with branding, nav items, theme toggle"
      min_lines: 50
    - path: "fintrack-dashboard/src/components/layout/mobile-nav.tsx"
      provides: "Fixed bottom tab bar for mobile"
      min_lines: 30
    - path: "fintrack-dashboard/src/components/layout/theme-toggle.tsx"
      provides: "Sun/moon icon toggle using next-themes"
      min_lines: 20
    - path: "fintrack-dashboard/src/app/(app)/layout.tsx"
      provides: "App shell layout with sidebar + main content area"
      min_lines: 15
    - path: "fintrack-dashboard/src/app/(app)/page.tsx"
      provides: "Dashboard placeholder page (Server Component)"
    - path: "fintrack-dashboard/src/app/(app)/transactions/page.tsx"
      provides: "Transactions placeholder page"
    - path: "fintrack-dashboard/src/app/(app)/recurring/page.tsx"
      provides: "Recurring placeholder page"
    - path: "fintrack-dashboard/src/app/(app)/chat/page.tsx"
      provides: "Chat placeholder page"
  key_links:
    - from: "fintrack-dashboard/src/app/(app)/layout.tsx"
      to: "fintrack-dashboard/src/components/layout/sidebar.tsx"
      via: "import and render in flex layout"
      pattern: "Sidebar.*hidden md:flex"
    - from: "fintrack-dashboard/src/app/(app)/layout.tsx"
      to: "fintrack-dashboard/src/components/layout/mobile-nav.tsx"
      via: "import and render with md:hidden"
      pattern: "MobileNav.*md:hidden"
    - from: "fintrack-dashboard/src/components/layout/sidebar.tsx"
      to: "fintrack-dashboard/src/components/layout/theme-toggle.tsx"
      via: "rendered at bottom of sidebar"
      pattern: "ThemeToggle"
    - from: "fintrack-dashboard/src/components/layout/sidebar.tsx"
      to: "usePathname"
      via: "pathname comparison for active state"
      pattern: "usePathname.*isActive"
---

<objective>
Build the sidebar navigation, mobile bottom tab bar, theme toggle, app layout shell, and placeholder pages for all 4 routes.

Purpose: This creates the navigation structure users interact with on every page. The sidebar/bottom-tab pattern and theme toggle are the primary UI affordances beyond content. All future phase work renders inside this layout shell.

Output: Complete navigation shell with responsive sidebar/bottom-tab, working theme toggle, and 4 placeholder pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-auth/01-CONTEXT.md
@.planning/phases/01-foundation-and-auth/01-RESEARCH.md
@.planning/phases/01-foundation-and-auth/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — contracts the executor needs -->

From fintrack-dashboard/src/components/theme-provider.tsx:
```typescript
export function ThemeProvider({ children, ...props }: React.ComponentProps<typeof NextThemesProvider>): JSX.Element
```

From fintrack-dashboard/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

shadcn/ui components available:
- Button: `import { Button } from "@/components/ui/button"`
- Separator: `import { Separator } from "@/components/ui/separator"`
- Skeleton: `import { Skeleton } from "@/components/ui/skeleton"`
- Sheet (for mobile drawer if needed): `import { Sheet, SheetContent, SheetTrigger } from "@/components/ui/sheet"`

CSS variables from globals.css:
- `--primary`: teal/cyan accent (used for active indicator bar)
- `bg-primary`: Tailwind class for accent color
- `text-muted-foreground`: inactive nav items
- `text-foreground`: active nav items

Icon library:
- lucide-react (installed by shadcn/ui)
- Available icons: LayoutDashboard, ArrowLeftRight, Repeat, MessageSquare, Sun, Moon
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build sidebar, mobile nav, theme toggle, and app layout</name>
  <files>
    fintrack-dashboard/src/components/layout/sidebar.tsx
    fintrack-dashboard/src/components/layout/mobile-nav.tsx
    fintrack-dashboard/src/components/layout/theme-toggle.tsx
    fintrack-dashboard/src/app/(app)/layout.tsx
    fintrack-dashboard/src/__tests__/sidebar.test.ts
    fintrack-dashboard/src/__tests__/theme-toggle.test.ts
  </files>
  <behavior>
    - Sidebar renders "FinTrack" branding at top
    - Sidebar renders 4 nav items: Dashboard (/), Transactions (/transactions), Recurring (/recurring), Chat (/chat)
    - Active nav item has a vertical teal/cyan accent bar on its left edge
    - Active nav item text is foreground color; inactive items are muted-foreground
    - Theme toggle at bottom of sidebar shows Sun icon in dark mode, Moon icon in light mode
    - Theme toggle click switches between dark and light themes
    - Mobile nav renders 4 icon-only tabs at the bottom of the screen
    - Mobile nav highlights active tab with primary color
  </behavior>
  <action>
    **Step 1 — Create the theme toggle component (src/components/layout/theme-toggle.tsx):**
    Follow RESEARCH.md Theme Toggle Component example exactly:
    - "use client" component
    - Uses `useTheme` from next-themes and `useState` for mounted check
    - Shows Sun icon when dark, Moon icon when light (lucide-react)
    - Uses shadcn Button with variant="ghost" size="icon"
    - Includes sr-only label "Toggle theme"
    - Returns placeholder button before mounted (prevents hydration mismatch)

    **Step 2 — Create the sidebar component (src/components/layout/sidebar.tsx):**
    - "use client" component (needs usePathname)
    - Full-height flex column: branding at top, nav items in middle, theme toggle at bottom
    - Branding: "FinTrack" text in bold, possibly with a small icon/logo
    - Nav items array:
      ```typescript
      const navItems = [
        { href: '/', icon: LayoutDashboard, label: 'Dashboard' },
        { href: '/transactions', icon: ArrowLeftRight, label: 'Transactions' },
        { href: '/recurring', icon: Repeat, label: 'Recurring' },
        { href: '/chat', icon: MessageSquare, label: 'Chat' },
      ]
      ```
    - Each nav item: Link component with icon + label, relative positioned
    - Active state detection: `usePathname()` compared against `href`
      - For root `/`: exact match
      - For others: `pathname.startsWith(href)`
    - Active indicator: absolute-positioned span on left edge, 3px wide, rounded-full, bg-primary, vertically centered (see RESEARCH.md Sidebar Active State pattern)
    - Active item styling: `text-foreground bg-accent/10`
    - Inactive item styling: `text-muted-foreground hover:text-foreground`
    - Sidebar width: ~240px (w-60) — Claude's discretion
    - Theme toggle rendered at bottom with `mt-auto` spacing
    - Separator between nav items and theme toggle

    **Step 3 — Create the mobile nav component (src/components/layout/mobile-nav.tsx):**
    - "use client" component (needs usePathname)
    - Fixed to bottom of screen: `fixed bottom-0 left-0 right-0`
    - Background matches card color with border-top
    - 4 icon-only tabs in a flex row, evenly spaced
    - Same nav items array as sidebar (extract to shared constant or repeat — Claude's discretion)
    - Active tab: icon in primary color, small dot indicator below
    - Inactive tab: icon in muted-foreground
    - z-index: 50 (above page content)
    - Safe area padding for iOS: `pb-safe` or similar

    **Step 4 — Create the app layout (src/app/(app)/layout.tsx):**
    - Server Component (no "use client" needed)
    - Flex layout: sidebar on left (hidden on mobile, visible on md+), main content area fills remaining space
    - Main content: `flex-1 overflow-auto` with padding
    - Mobile nav: visible on mobile (md:hidden), hidden on desktop
    - Bottom padding on main for mobile to account for fixed bottom nav
    - Structure per RESEARCH.md Pattern 1 (Route Group for Auth Layout)

    **Step 5 — Implement the test stubs:**
    Replace `it.todo()` in sidebar.test.ts with actual tests:
    - Test that sidebar renders 4 nav items with correct labels
    - Test that sidebar renders "FinTrack" branding text
    - Mock usePathname to test active state highlighting

    Replace `it.todo()` in theme-toggle.test.ts with actual tests:
    - Test that toggle button renders
    - Test that clicking toggles between themes (mock useTheme)
    - Test that sr-only label exists

    **Design details from CONTEXT.md:**
    - Copilot-style: warm, rounded, soft gradients
    - Reference design-system/MASTER.md for specific spacing/color guidance
    - Icons from lucide-react (LayoutDashboard, ArrowLeftRight, Repeat, MessageSquare)
    - Transition effects on hover (150-300ms per UI UX Pro Max skill guidelines)
    - Cursor pointer on all clickable elements
  </action>
  <verify>
    <automated>cd fintrack-dashboard && npx vitest run src/__tests__/sidebar.test.ts src/__tests__/theme-toggle.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - Sidebar renders with FinTrack branding, 4 nav items, theme toggle at bottom
    - Active page has vertical teal accent bar indicator
    - Mobile bottom tab bar renders on small viewports
    - Theme toggle switches dark/light mode
    - All sidebar and theme-toggle tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create placeholder pages for all 4 routes</name>
  <files>
    fintrack-dashboard/src/app/(app)/page.tsx
    fintrack-dashboard/src/app/(app)/transactions/page.tsx
    fintrack-dashboard/src/app/(app)/recurring/page.tsx
    fintrack-dashboard/src/app/(app)/chat/page.tsx
  </files>
  <action>
    Move the existing root page.tsx into the (app) route group and create the other 3 page files.

    **Each page should be a Server Component** (no "use client") with:
    - Page title as an h1 heading
    - A brief placeholder message (e.g., "Your dashboard overview will appear here")
    - Use shadcn Skeleton components to show a preview of what the future content layout will look like
    - Each page should feel intentional, not broken — the user should understand content is coming

    **Specific placeholder content:**
    - Dashboard (`/`): Title "Dashboard", skeleton cards in a grid layout hinting at account cards and charts
    - Transactions (`/transactions`): Title "Transactions", skeleton table rows hinting at a transaction list
    - Recurring (`/recurring`): Title "Recurring", skeleton list items hinting at recurring charges
    - Chat (`/chat`): Title "Chat", skeleton message bubbles hinting at a chat interface

    **Important:** Keep all pages as Server Components. No state, no interactivity, no "use client". These are pure layout placeholders.

    **Move the existing root page.tsx:**
    The current `src/app/page.tsx` (created in Plan 01 as a temporary "FinTrack" page) needs to be moved to `src/app/(app)/page.tsx` and replaced with the Dashboard placeholder. The root-level `src/app/page.tsx` should be deleted or the (app) route group's page.tsx takes over the `/` route.
  </action>
  <verify>
    <automated>cd fintrack-dashboard && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - All 4 routes render: /, /transactions, /recurring, /chat
    - Each page shows its title and skeleton placeholder content
    - All pages are Server Components (no "use client" directive)
    - Build succeeds with no errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 1 UI</name>
  <action>Human verifies the complete Phase 1 UI works correctly across all flows.</action>
  <what-built>Complete app shell with sidebar navigation, mobile bottom tabs, theme toggle, login page, and placeholder pages — the entire Phase 1 UI.</what-built>
  <how-to-verify>
    1. Start the dev server: `cd fintrack-dashboard && npm run dev`
    2. Open http://localhost:3000 — should redirect to /login
    3. Enter the password from APP_PASSWORD env var — should redirect to dashboard
    4. Verify sidebar shows: FinTrack logo, Dashboard (active with teal bar), Transactions, Recurring, Chat, theme toggle at bottom
    5. Click each nav item — page changes, active indicator moves
    6. Click theme toggle — switches between dark and light mode
    7. Refresh the page — theme preference persists
    8. Resize browser to mobile width — sidebar disappears, bottom tab bar appears with 4 icons
    9. Verify Satoshi font is rendering (check in browser devtools > Computed > font-family)
    10. Open a new incognito window and visit http://localhost:3000 — should redirect to /login (no session)
  </how-to-verify>
  <verify>Human visual and functional verification</verify>
  <done>User confirms auth flow, navigation, theme toggle, mobile layout, and font rendering all work correctly</done>
  <resume-signal>Type "approved" or describe any issues with the navigation, theme, auth flow, or mobile layout</resume-signal>
</task>

</tasks>

<verification>
1. `cd fintrack-dashboard && npx vitest run --reporter=verbose` — all tests pass (middleware, auth API, sidebar, theme toggle)
2. `cd fintrack-dashboard && npm run build` — succeeds
3. Navigation works for all 4 routes with active state indicators
4. Theme toggle persists preference across page reloads
5. Mobile bottom tab bar replaces sidebar on small viewports
6. Auth gate blocks unauthenticated access
</verification>

<success_criteria>
- Sidebar with FinTrack branding, 4 nav items with icons/labels, teal accent bar on active item
- Mobile bottom tab bar with 4 icons, visible only on small viewports
- Theme toggle at bottom of sidebar, sun/moon icons, preference persists to localStorage
- App layout shell with sidebar + scrollable main content area
- 4 placeholder pages as Server Components with skeleton previews
- Human verified the complete Phase 1 UI works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-auth/01-03-SUMMARY.md`
</output>
