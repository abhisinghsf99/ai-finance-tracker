import { describe, it } from "vitest"

describe("auth API route", () => {
  it.todo("returns 401 for wrong password")
  it.todo("returns 200 and sets cookie for correct password")
  it.todo("returns 401 for missing password")
})
