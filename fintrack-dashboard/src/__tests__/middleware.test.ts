import { describe, it } from "vitest"

describe("middleware", () => {
  it.todo("redirects unauthenticated requests to /login")
  it.todo("allows requests with valid session cookie")
  it.todo("allows /login and /api/auth without cookie")
})
